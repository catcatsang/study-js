// 댓글 모듈 만들기(resplyservice)
// 작성, 목록, 수정, 삭제제

const resplyservice = (() => {
  const write = (post) => {};
  const getlist = (post) => {
    callback("내용용");
  };
  const update = (id, post) => {};
  const remove = (id) => {};
})();
